@extends('layouts.app')
@section('content')<section class="site-container listing-page"><div class="listing-intro"><div><span class="section-kicker">Harian Merah Putih</span><h1>{{ $title }}</h1></div><p>Modul publik {{ strtolower($kind) }} dengan status terbit.</p></div><div class="listing-grid">@forelse($items as $item)<article class="article-card">@if($item->media)<img class="showcase-image" src="{{ $item->media->url() }}" alt="{{ $item->media->alt_text ?: $item->title }}">@else<x-news-media />@endif<div class="article-card__body"><span class="story-meta__category">{{ $kind }}</span><h2>{{ $item->title }}</h2><p>{{ $item->excerpt }}</p><div class="article-card__meta"><time>{{ $item->published_at?->locale('id')->translatedFormat('d F Y') }}</time></div></div></article>@empty<x-empty-state title="Belum ada konten" />@endforelse</div><div class="pagination-wrap">{{ $items->links() }}</div></section>@endsection
